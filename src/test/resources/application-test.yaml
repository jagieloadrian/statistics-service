ktor:
  application:
    modules:
      - com.anjo.statisticservice.ApplicationKt.module
  deployment:
    port: 8080
    environment: test
  redis:
    port: ${REDIS_PORT:6379}
    host: localhost
    password: "${REDIS_PASSWORD:default}"
    timeout: 10
    client-name: statistic-service
    ssl: false